# 528实验室服务器使用完整指南

## 目录

- [服务器连接](#服务器连接)
- [Linux基础操作](#linux基础操作)
- [作业调度系统](#作业调度系统)
- [计算软件使用](#计算软件使用)
- [可视化和分析工具](#可视化和分析工具)
- [参考资料](#参考资料)

## 服务器连接

### ❗用前须知

只有连接了学校**校园网** OR 实验室的 **WIFI** 才可以连接入实验室的服务器！！

（内网穿透涉及隐私、安全、法律等问题，请勿私自对实验室的服务器做修改）

### 推荐SSH软件(以Windows设备为主)

**推荐选项：**
- **xshell + xftp**
- **MobaXterm**

### 连接方法

#### 基础语法
```bash
ssh mmm@[询问管理员获取IP地址]
```

#### 1. 命令行连接 (cmd/Terminal)
- ✅ 可以很方便的通过Windows操作Linux服务器
- ❌ 无法与服务器之间互相传递文件

#### 2. MobaXterm连接
1. 点击 `Session` -> `SSH`，进入SSH设置页面
2. 填入支持SSH协议的主机IP和用户名
3. 点击页面下方OK按钮，完成配置
4. 输入ssh用户的密码
5. 连接SSH成功后，会自动连接SFTP
6. 可以设置主密码来保存各个连接的密码

## Linux基础操作

### 系统包管理 (apt命令)

apt (Advanced Packaging Tool) 是 Ubuntu 系统中的软件包管理器。

#### 基本语法
```bash
sudo apt [选项] [命令] [包名]
```

#### 常用命令
```bash
# 更新软件包列表
sudo apt update

# 升级已安装的软件包
sudo apt upgrade

# 安装软件包
sudo apt install package_name

# 删除软件包
sudo apt remove package_name

# 搜索软件包
apt search keyword

# 显示包信息
apt show package_name

# 清理系统
sudo apt autoremove
sudo apt autoclean
```

### 文本编辑器

#### nano编辑器 (推荐新手使用)

**基本操作：**
```bash
# 打开/创建文件
nano filename

# 常用快捷键
Ctrl + O    # 保存文件
Ctrl + X    # 退出编辑器
Ctrl + W    # 搜索文本
Ctrl + K    # 删除当前行
Ctrl + U    # 粘贴已删除的行
```

#### vim编辑器 (高级用户)

**模式切换：**
- 普通模式 -> 插入模式：按 `i`
- 插入模式 -> 普通模式：按 `Esc`

**基本操作：**
```bash
# 打开文件
vim filename

# 保存并退出
:wq

# 不保存退出
:q!

# 搜索
/search_term
```

### 文件系统操作

#### 导航命令
```bash
pwd         # 显示当前目录
ls          # 列出文件和目录
ls -la      # 详细列出所有文件
cd dir      # 切换到目录
cd ..       # 返回上级目录
cd ~        # 返回主目录
```

#### 文件操作
```bash
cp file1 file2      # 复制文件
mv file1 file2      # 移动/重命名文件
rm file             # 删除文件
rm -rf directory    # 删除目录及其内容
mkdir dir           # 创建目录
```

#### 查看文件内容
```bash
cat file        # 显示文件内容
less file       # 分页显示文件内容
head file       # 显示文件前10行
tail file       # 显示文件后10行
tail -f file    # 实时显示文件新增内容
```

#### 权限管理
```bash
chmod +x file       # 给文件添加执行权限
chmod 755 file      # 设置文件权限
chown user file     # 改变文件所有者
```

### 系统监控

```bash
top         # 显示系统进程
htop        # 更友好的进程显示
ps aux      # 显示所有进程
free -h     # 显示内存使用情况
df -h       # 显示磁盘使用情况
```

## 作业调度系统

### SLURM基础命令

```bash
# 查看队列状态
sinfo

# 查看用户作业
squeue -u $USER

# 提交作业
sbatch script.sh

# 取消作业
scancel job_id

# 查看作业详情
scontrol show job job_id

# 查看作业历史
sacct -u $USER
```

### 作业脚本模板

```bash
#!/bin/bash
#SBATCH -J job-name           # 作业名称
#SBATCH -o %j.out            # 标准输出文件
#SBATCH -e %j.err            # 标准错误文件
#SBATCH -p partition         # 分区名称
#SBATCH -N 1                 # 节点数量
#SBATCH -n 32                # CPU核数
#SBATCH --mem=20G            # 内存需求
#SBATCH -t 24:00:00          # 时间限制

# 你的计算命令
```

## 计算软件使用

### Gaussian
- 📁 详细使用指南：`02-Gaussian_Manual/Gaussian.md`
- 🔗 参考手册：`0000-计算化学必读书目/Gaussian/`

### VASP
- 📁 详细使用指南：`03-VASP_Manual/VASP.md`
- 📁 脚本工具：`03-VASP_Manual/VASP脚本工具总结.md`
- 🔗 参考手册：`0000-计算化学必读书目/VASP/`

### ORCA
- 📁 使用指南：`04-ORCA_Manual/ORCA.md`

### LAMMPS
- 📁 使用指南：`07-LAMMPS_Manual/LAMMPS.md`

### GROMACS
- 📁 使用指南：`08-GROMACS_Manual/GROMACS.md`

## 可视化和分析工具

### VMD (分子可视化)
- 📁 使用指南：`05-VMD/VMD.md`
- 📖 中文教程：`0000-计算化学必读书目/VMD教程中文版.pdf`
- 📖 用户手册：`0000-计算化学必读书目/VMD用户手册.pdf`

### Multiwfn (波函数分析)
- 📁 使用指南：`06-Multiwfn/Multiwfn.md`
- 📖 详细手册：`0000-计算化学必读书目/Multiwfn_Manual_2.3.2.pdf`

### PyMOL (分子可视化)
- 📁 使用指南：`09-PyMOL_Manual/PyMOL.md`

## 参考资料

### 📚 计算化学必读书目
- **量子化学理论**：`0000-计算化学必读书目/量子化学中的计算方法.pdf`
- **软件手册合集**：`0000-计算化学必读书目/`目录下各软件的官方手册

### 🔧 实用工具
- **SLURM用户指南**：`01-SSH_Link/slurm-userguide.pdf`
- **VASPKIT工具**：`03-VASP_Manual/VASPKIT中文手册.pdf`

### 💡 使用技巧
1. **文件传输**：建议使用MobaXterm的SFTP功能
2. **长时间计算**：使用SLURM提交后台作业
3. **进度监控**：使用`tail -f`命令监控输出文件
4. **存储管理**：定期清理临时文件，压缩大文件
5. **备份重要**：重要结果及时备份到本地

### ⚠️ 注意事项
1. 服务器资源有限，请合理使用
2. 不要在登录节点运行大型计算
3. 定期检查磁盘使用情况
4. 遵守实验室服务器使用规范
5. 遇到问题及时联系管理员

---

**最后更新**：2025年7月26日  
**维护者**：528实验室  
**联系方式**：询问实验室管理员
